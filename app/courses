export default function Courses() {
  const courses = [
    { name: "SMC Gold Masterclass", status: "Available" },
    { name: "MMPRA™ Advanced Setup", status: "VIP Only" },
    { name: "Liquidity Analysis Workshop", status: "Coming Soon" },
  ];

  return (
    <div className="min-h-screen bg-black text-white p-10">
      <h1 className="text-4xl font-bold text-gold text-center mb-16">MTSO Academy Courses</h1>
      <div className="grid gap-6 max-w-4xl mx-auto">
        {courses.map((course, i) => (
          <div key={i} className="bg-neutral-900 p-6 rounded-xl border border-neutral-800 flex justify-between items-center">
            <span className="text-lg font-bold">{course.name}</span>
            <span className="text-gold border border-gold px-3 py-1 rounded text-sm">{course.status}</span>
          </div>
        ))}
      </div>
    </div>
  );
}
